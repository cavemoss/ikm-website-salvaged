{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --host",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@vueuse/core": "^10.11.1",
    "nuxt": "^3.12.3",
    "vue": "latest"
  },
  "devDependencies": {
    "nuxt-particles": "^0.3.0",
    "sass": "^1.77.8"
  }
}
